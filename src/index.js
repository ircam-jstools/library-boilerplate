/**
 * Documentation example for myLibrary.
 *
 * @example
 * import myLibrary from 'my-library';
 *
 * myLibrary.getTrue();
 * > true
 */
const myLibrary = {
  /**
   * Function that returns `true`
   * @return {Boolean}
   */
  getTrue() {
    return true;
  }
}

export default myLibrary;
